[package]
name = "algo_launcher"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { path = "../shared_rust", features = ["client"] }
anyhow = "1.0"
axum = { version = "0.7", features = ["ws"] }
tokio = { version = "1", features = ["full"] }
tokio-tungstenite = "0.24"
